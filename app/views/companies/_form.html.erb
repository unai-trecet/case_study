<%= form_with(model: company, local: true, class: "form-group") do |form| %>
  <% if company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field", class: "form-group">
    <%= form.label :name %>
    <%= form.text_field :name, id: :company_name, class: "form-group" %>
  </div>

  <div class="field">
    <%= form.label :identify %>
    <%= form.text_field :identify, id: :company_identify, class: "form-group" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
